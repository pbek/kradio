0.3.0-alpha2

       translation preparation                  // done
       german translation
       segfault fixes with 2.95.3
       tooltips in config dialog
       lasterror für radiodevice und recording
       v4l2 support (implemented, hardly tested) // done
       recording button: +menu for recording/monitoring // done
       symbol for station search button         // done
       use special mail-adress for preset files // done
       country/city in preset file mail         // done
       about dialog                             // done
       convert preset files                     // done
       converter: add xml-encoding-line         // done
       font selector for display                // done
       optional inactive text color             // done
       recording icon improvements (whiter)     // done

0.3.0-alpha3

       optimize recording data monitor (repaint only what is necessary)
       option for display window type (entry in taskbar or toolbar window)
       mp3 encoding with liblame_enc
       background pixmap for display
       .so-library plugins
       plugin config
       lirc config
       internet radio streams
       translations

0.3.0-beta1
  
       gui-errormessages

       tooltips complete?
       prepare for translation: check for missing i18n
       translations?
       

future:
* mediacontrol applet integration
* dcop interface
